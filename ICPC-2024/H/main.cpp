#include <bits/stdc++.h>
#define REP(i,a,b)for(int i =a; i < b; i++)
using namespace std;
void _solve(){

    int n, k;
    cin >> n >> k;
    vector<vector<int>> presupuesto(n);
    REP(i, 0, n){
        int p;
        cin >> p;
        vector<int> aux(p);
        REP(i, 0, p){
            cin >> aux[p]
        }
        presupuesto[i] = aux;
    }
    REP(i, 0, n){

    }

}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    _solve();
    return 0;
}
